# Cinema Website Project (Dwayne Cinema)

Веб-приложение для кинотеатра с имитацией бэкенда, системой авторизации, корзиной и мульти-язычностью.

## 🚀 Основные Возможности

### 1. Система Авторизации (Mock Auth)
*   **Регистрация/Вход:** Данные пользователей сохраняются в `localStorage`.
*   **Гостевой режим:** Доступ к сайту без входа. В профиле отображается просьба войти.
*   **Личный кабинет:**
    *   Просмотр Email, статуса Bonus Club и баллов лояльности.
    *   История покупок.
    *   Настройки (смена пароля - имитация).

### 2. Интерактивный Каталог
*   **Динамический рендеринг:** Карточки фильмов генерируются через JS.
*   **Фильтрация:** Поиск по названию и категориям (Популярное/Рекомендуемое).
*   **Корзина:** Добавление билетов, изменение количества, подсчет суммы.

### 3. Локализация и Настройки
*   **Мультиязычность:** Поддержка RU/EN/KK (данные берутся из словаря в JS).
*   **Выбор Города:** Цены и наличие кинотеатров зависят от выбранного города.
*   **Сохранение состояния:** Выбранный язык и город запоминаются в браузере.

## 📂 Структура Проекта

*   **`index.html`**: Главная страница (Лендинг).
*   **`pages/`**:
    *   `bin.html`: Страница Входа и Регистрации.
    *   `profile.html`: Личный кабинет пользователя.
    *   `catalog.html`: Полный список фильмов.
    *   `contacts.html`: Список кинотеатров (фильтруется по городу) и форма связи.
    *   `about.html`: О компании.
*   **`js/script.js`**: Единый файл логики (SPA-like подход на MPA).
*   **`css/`**: Стили разбиты на модули (`reset`, `base`, `layout`, `components`).

## 🛠 Технический Стек

*   **Frontend:** HTML5, CSS3 (Flexbox/Grid), Vanilla JavaScript (ES6+).
*   **Data:** JSON-объекты прямо в коде (`films`, `cities`, `translations`).
*   **Storage:** `localStorage` для персистенции данных (пользователи, настройки).

## 📦 Запуск

1.  Клонируйте репозиторий или скачайте архив.
2.  Откройте файл `index.html` в любом современном браузере.
3.  Для корректной работы навигации лучше использовать локальный сервер (например, Live Server в VS Code), но и простое открытие файла будет работать.

## 📝 Авторы
Дамаев Тамерлан, Сериков Шахан